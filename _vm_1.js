  !function(){    // v1.0.358: 修复12强杯赛 container 致命bug——_c12SelectTeam/submitCupMatchResult12 写入 scheduleContent 而非 standingsContent，导致选队/录入比分后对阵图不刷新；修复 submitCupMatchResult12 中 inp.datasetpid 笔误    var _upgraded = sessionStorage.getItem('pgm_upgraded');    if (_upgraded) {      // 上一次是升级跳转来的，清除 SW 缓存并移除标记      sessionStorage.removeItem('pgm_upgraded');      Promise.resolve().then(function(){        var tasks = [];        if('serviceWorker' in navigator){tasks.push(navigator.serviceWorker.getRegistrations().then(function(rs){return Promise.all(rs.map(function(r){return r.unregister()}))}))}        if('caches' in window){tasks.push(caches.keys().then(function(ns){return Promise.all(ns.map(function(n){return caches.delete(n)}))}))}        return Promise.all(tasks);      }).then(function(){        var baseUrl = location.href.split('?')[0].split('#')[0];        var newUrl = baseUrl + '?_v=' + Date.now();        location.replace(newUrl);      });      return;    }    fetch('version.json?t='+Date.now(),{cache:'no-store'}).then(function(r){return r.json()}).then(function(d){      // 公告条      if(d.notice&&d.notice.trim()){var b=document.createElement('div');b.style.cssText='position:fixed;top:0;left:0;right:0;background:linear-gradient(135deg,#e91e63,#ff5722);color:#fff;text-align:center;padding:10px 16px;font-size:14px;font-weight:bold;z-index:999999;';var _ns=document.createElement('span');_ns.textContent='📢 '+d.notice;var _nw=document.createElement('div');_nw.style.cssText='max-width:600px;margin:0 auto;';_nw.appendChild(_ns);b.appendChild(_nw);document.body.prepend(b)}      // 🛡️ 防止升级后循环弹框：刚完成升级则跳过本次检查      if(sessionStorage.getItem('pgm_upgraded')==='1'){ sessionStorage.removeItem('pgm_upgraded')      } else {      var needUpgrade=false      try{      var myV=0;try{var parts2='1.0.433'.split('.').map(Number);myV=(parts2[0]||0)*10000+(parts2[1]||0)*100+(parts2[2]||0)}catch(e){}      var parts=d.minVersion?d.minVersion.split('.').map(Number):[]      var minV=parts.length?((parts[0]||0)*10000+(parts[1]||0)*100+(parts[2]||0)):myV      if(myV<minV) needUpgrade=true      }catch(e){}      if(needUpgrade){          // 创建升级提示框          var overlay = document.createElement('div');          overlay.id = 'versionUpdateOverlay';          overlay.style.cssText = 'position:fixed;top:0;left:0;right:0;bottom:0;background:rgba(0,0,0,0.6);z-index:9999998;display:flex;align-items:center;justify-content:center;';          overlay.innerHTML = '<div style="background:#fff;border-radius:16px;padding:32px 28px;max-width:360px;width:90%;text-align:center;box-shadow:0 20px 60px rgba(0,0,0,0.4);">' +            '<div style="font-size:52px;margin-bottom:12px;">🚀</div>' +            '<div style="font-size:20px;font-weight:bold;color:#1B5E20;margin-bottom:6px;">发现新版本</div>' +            '<div style="font-size:13px;color:#666;margin-bottom:20px;line-height:1.7;">' +              '当前版本：v' + parts2.join('.') + '<br>' +              '最新版本：<b style="color:#e91e63;">v' + d.minVersion + '</b><br>' +              '点击下方按钮更新到最新版本' +            '</div>' +            '<button id="btnUpdateNow" style="width:100%;background:linear-gradient(135deg,#1B5E20,#2E7D32);color:#fff;border:none;padding:13px 0;border-radius:50px;font-size:15px;font-weight:bold;cursor:pointer;margin-bottom:10px;box-shadow:0 4px 12px rgba(27,94,32,0.35);">🚀 立即更新</button>' +            '<button id="btnUpdateLater" style="width:100%;background:#f5f5f5;color:#999;border:none;padding:11px 0;border-radius:50px;font-size:14px;cursor:pointer;">稍后再说</button>' +          '</div>';          document.body.appendChild(overlay);          // 立即更新按钮          document.getElementById('btnUpdateNow').onclick = function(){            var btn = this;            btn.disabled = true;            btn.textContent = '⏳ 正在清理缓存...';            // 先标记升级状态            try { sessionStorage.setItem('pgm_upgraded', '1'); } catch(e){}            var tasks = [];            if('serviceWorker' in navigator){tasks.push(navigator.serviceWorker.getRegistrations().then(function(rs){return Promise.all(rs.map(function(r){return r.unregister()}))}))}            if('caches' in window){tasks.push(caches.keys().then(function(ns){return Promise.all(ns.map(function(n){return caches.delete(n)}))}))}            Promise.all(tasks).then(function(){              var baseUrl = location.href.split('?')[0].split('#')[0];              var newUrl = baseUrl + '?_v=' + Date.now();              location.replace(newUrl);            }).catch(function(){              var baseUrl = location.href.split('?')[0].split('#')[0];              var newUrl = baseUrl + '?_v=' + Date.now();              location.replace(newUrl);            });          };          // 稍后再说按钮          document.getElementById('btnUpdateLater').onclick = function(){            overlay.remove();          };        }      }    }).catch(function(){})  }();  